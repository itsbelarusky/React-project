import React from 'react';
import s from './Footer.module.css';

const Footer = (props) => {
    return (
        <main className={s.footer}>
            <span>Footer</span>
        </main>


    );
};

export default Footer;